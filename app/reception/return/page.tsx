import { ComingSoon } from "@/components/ComingSoon";

export default function ReturnPage() {
  return (
    <ComingSoon
      title="Receive Return"
      note="Wires up once the return flow (Phase 12) exists — this will confirm physical receipt and move the booking to Awaiting Inspection."
    />
  );
}
