import { ComingSoon } from "@/components/ComingSoon";

export default function BatteryExchangePage() {
  return (
    <ComingSoon
      title="Battery Exchange"
      note="Wires up once the battery exchange flow (Phase 15) exists — scan the old battery, then the replacement."
    />
  );
}
