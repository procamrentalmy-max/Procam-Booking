import { ComingSoon } from "@/components/ComingSoon";

export default function PickupPage() {
  return (
    <ComingSoon
      title="Check Customer Pickup"
      note="Wires up once the booking engine (Phase 6) exists — this will look up a booking by code and show which camera/kit to hand over."
    />
  );
}
